# Internal Benchmark Slices

These are tiny, checked-in internal research slices.

Rules:
- benchmark-only
- not replacements for the full internal non-demo corpus
- keep provenance explicit
- prefer public or already-published artifacts only

The full internal benchmark path still comes from:
- `training/replay_pack/non_demo_eval.jsonl`
- frozen SimSat captures
- `training/scripts/materialize_internal_benchmark_slice.py`
